import { BoardBox } from "@/domains/Board/components";
import type { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <>
            <BoardBox />
        </>
    );
};

export default Home;
